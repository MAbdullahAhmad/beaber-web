import { defineConfig } from 'vite';
export default defineConfig({ base:'/login/', server:{ host:'127.0.0.1', port:4010, strictPort:true }, preview:{ host:'127.0.0.1', port:4010, strictPort:true } });
